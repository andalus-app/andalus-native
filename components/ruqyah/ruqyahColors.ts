/**
 * Ruqyah brand colors — dark navy palette with amber/orange accent.
 * Matched to islam.nu web color scheme.
 */

// ── Accent ───────────────────────────────────────────────────────────────────
export const RO           = '#F6A15B';                      // primary orange
export const RO_DARK      = '#E08C44';                      // pressed state
export const RO_DIM       = 'rgba(246,161,91,0.14)';        // glow bg
export const RO_DIM_STRONG= 'rgba(246,161,91,0.22)';        // stronger glow
export const RO_TEXT_ON   = '#1C0E00';                      // text on orange bg

// ── Dark background palette ───────────────────────────────────────────────────
export const RO_BG        = '#0C1120';                      // screen bg (deep navy)
export const RO_SURFACE   = '#141C30';                      // cards, panels
export const RO_CHIP      = '#1A2340';                      // chip/pill bg (separates from surface)

// ── Borders ───────────────────────────────────────────────────────────────────
export const RO_BORDER       = 'rgba(246,161,91,0.42)';     // clickable element rim
export const RO_BORDER_FAINT = 'rgba(246,161,91,0.16)';     // dividers / subtle

// ── Text ─────────────────────────────────────────────────────────────────────
export const RO_TEXT         = '#FFFFFF';
export const RO_TEXT_SEC     = 'rgba(255,255,255,0.72)';    // body / secondary
export const RO_TEXT_MUTED   = 'rgba(255,255,255,0.42)';    // captions, counts
